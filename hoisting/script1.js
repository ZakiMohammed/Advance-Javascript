x = 5;

console.log(x);

var x;  // js hoisting

/**
 * Hoisting (raise (something) by means of ropes and pulleys)
 * - JS pull the declaration to the topby default
 * - It pulls the declaration not initialization
 * - let and const are not hoisted
 */